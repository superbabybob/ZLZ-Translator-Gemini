# คู่มือติดตั้งและใช้งานแบบละเอียด (ทีละคลิก)

เครื่องมือแปลภาษาและช่วยสื่อสารสำหรับคนทำงาน/ฟรีแลนซ์ไทย แปลผ่าน **Google Gemini API** (เร็วเฉลี่ย ~1 วินาที มีโควต้าฟรี 1,500 ครั้ง/วัน)

---

## ขั้นที่ 1: ติดตั้งและเปิดโปรแกรม (ทำครั้งเดียว, 1 นาที)

1. ดับเบิลคลิก **`setup.bat`** (หรือดับเบิลคลิก **`run_hotkey.bat`** ได้ทันที ระบบจะติดตั้งให้อัตโนมัติในครั้งแรก)
2. เมื่อติดตั้งเสร็จ ตัวโปรแกรมจะสร้าง Shortcut ไว้บน Desktop และเปิดโปรแกรมขึ้นมา
3. หากระบบยังไม่พบ `GEMINI_API_KEY` จะมีหน้าต่าง **"ตั้งค่าคีย์และบัญชี"** เด้งขึ้นมาให้อัตโนมัติ:
   - กดปุ่ม **"🌐 เปิดหน้าขอคีย์ Gemini"** (เบราว์เซอร์จะเปิดหน้า aistudio.google.com/apikey ให้ล็อกอินบัญชี Google)
   - กด **Create API key** เพื่อก๊อปคีย์ฟรี
   - นำคีย์มาวางในช่อง `GEMINI_API_KEY` แล้วกด **บันทึกแล้วทดสอบแปล**
   - เมื่อขึ้นข้อความสีเขียวว่า "ใช้ได้" ก็ปิดหน้าต่างและเริ่มใช้งานได้ทันที!

---

## ขั้นที่ 2: ใช้งานปุ่มลัด (Hotkey)

1. ดับเบิลคลิก **`run_hotkey.bat`**
2. จะมี **ไอคอนสี่เหลี่ยมสีน้ำเงิน** ที่มุมขวาล่างของจอ (system tray)
   - ถ้าไม่เห็น ให้คลิกลูกศร **^** ตรง tray ไอคอนอาจซ่อนอยู่ในนั้น ลากออกมาวางไว้ข้างนอกได้
3. **ลองแปลข้อความที่ลูกค้าส่งมา**
   - เปิด Discord หรือเบราว์เซอร์ ลากเมาส์คลุมข้อความภาษาอังกฤษให้เป็นแถบสี
   - กด **F8** แล้วปล่อยมือ
   - หน้าต่างดำจะเด้งขึ้นข้างเมาส์ บรรทัดบนคือต้นฉบับ บรรทัดล่างคือคำแปลไทย
   - ปิดด้วยปุ่ม ✕ หรือกด Esc
4. **ลองตอบลูกค้า**
   - คลิกในช่องพิมพ์ข้อความของ Discord / โปรแกรมแชท
   - พิมพ์ภาษาไทย เช่น `ได้ครับ น่าจะส่งให้ได้พรุ่งนี้เช้า`
   - กด **F9** แล้วปล่อยมือ
   - ภายใน ~1 วินาที ข้อความไทยในช่องจะถูกแทนที่ด้วยภาษาอังกฤษทันที และมีหน้าต่างเล็กบอก "ลูกค้าจะอ่านว่า" อะไร แปลกลับเป็นไทยให้ตรวจเช็กความหมายก่อนกด Enter ส่ง
5. **ถ้าอ่านแล้วยังงงเจตนาลูกค้า** ลากคลุมข้อความลูกค้าแล้วกด **Shift + F8** จะได้คำอธิบายว่าลูกค้าพูดเรื่องอะไร ต้องการให้ทำอะไร และน้ำเสียงเป็นยังไง
6. **ถ้าพิมพ์อังกฤษเองแต่ไม่มั่นใจ** พิมพ์ในช่องแชทแล้วกด **Shift + F9** จะแก้ไวยากรณ์และสำนวนให้เป็นธรรมชาติ
7. **ให้เปิดเองตอนเปิดเครื่อง**: คลิกขวาไอคอน tray ติ๊ก **เปิดอัตโนมัติเมื่อเข้า Windows**
8. **เปิด/ปิดชั่วคราว**: คลิกซ้ายที่ไอคอน tray หนึ่งครั้ง (ไอคอนสีเทา = ปิดอยู่, คลิกอีกครั้งเพื่อเปิด)

---

## ขั้นที่ 3: ตั้งค่า Discord App (ทางเลือก, 5 ถึง 10 นาที)

ทำให้คลิกขวาที่ข้อความใน Discord แล้วแปลได้เลย และใช้จากมือถือได้ (ตอน PC เปิดโปรแกรมอยู่)

### 3.1 สร้างแอปใน Discord Developer Portal
1. เปิด https://discord.com/developers/applications ล็อกอินบัญชี Discord
2. กดปุ่ม **New Application** ตั้งชื่อ เช่น `Translator` แล้วกด **Create**

### 3.2 เปิดให้ติดตั้งแบบ User Install
1. เมนูด้านซ้าย คลิก **Installation**
2. หัวข้อ **Installation Contexts** ติ๊ก **User Install**
3. หัวข้อ **Install Link** เลือก **Discord Provided Link**
4. กด **Save Changes** แล้วก๊อป **Install Link** เก็บไว้

### 3.3 เอา Token มาใส่ในโปรแกรม
1. เมนูด้านซ้าย คลิก **Bot**
2. กด **Reset Token** แล้วกด **Yes, do it!**
3. กด **Copy** ใต้ token
4. คลิกขวาไอคอน tray เลือก **ตั้งค่าคีย์และบัญชี...** วาง token ในช่อง `DISCORD_TOKEN` แล้วกดบันทึก

### 3.4 ติดตั้งแอปเข้าบัญชี Discord ของคุณ
1. เปิดลิงก์ Install Link ในเบราว์เซอร์
2. เลือก **Add to My Apps** แล้วกด **Authorize**
3. ใน Discord สามารถพิมพ์ `/en` หรือคลิกขวาที่ข้อความ -> Apps เพื่อแปลได้ทันที

---

## ปรับให้แปลตรงใจ

- **`glossary.md`** เปิดด้วย Notepad เติมชื่อโปรเจกต์ ศัพท์ที่ห้ามแปล และแนวทางน้ำเสียง บันทึกแล้วเลือก "โหลดการตั้งค่าใหม่" ที่ tray
- **`config.toml`**
  - `default_tone`: น้ำเสียงเริ่มต้น `friendly` / `formal` / `brief`
  - `[modes]`: เลือกรุ่น Gemini เช่น `gemini-3.6-flash` หรือ `gemini-2.5-pro`
  - `[hotkeys]`: ปรับเปลี่ยนปุ่มลัดได้ตามต้องการ
